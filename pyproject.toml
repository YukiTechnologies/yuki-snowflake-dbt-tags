[project]
name = "yuki-snowflake-dbt-tags"
version = "0.2.6"
description = "Dev environment for the yuki_snowflake_dbt_tags dbt package"
requires-python = ">=3.11"
dependencies = [
    "dbt-snowflake>=1.8",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
